name: panels
description: Panels is a flutter package that aims to provide a set of useful desktop UI panels that can move around, dock and tab like we expect in more robust desktop applications.
version: 0.0.2

repository: https://github.com/BlankSTDIO/panels
homepage: https://github.com/BlankSTDIO/panels
issue_tracker: https://github.com/BlankSTDIO/panels/issues

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

